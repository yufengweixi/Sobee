# Sobee 使用说明

Sobee 是一款 Windows 桌面快捷启动工具，同时提供护眼调色、快捷关闭屏幕、JS 脚本执行、主进程启动、HTML 窗口化等功能。

本文介绍 Sobee 的主要功能及使用方法。

---

## 1. 软件介绍

Sobee 可以将常用的：

- 软件
- 文件夹
- 文件
- UWP 应用
- 网站
- 系统命令
- JS 脚本

统一整理到悬浮面板中。

通过「类目 + 快捷方式」的方式管理，让常用工具更加集中，减少桌面和开始菜单中的查找操作。

软件默认常驻系统托盘，可以通过全局快捷键快速呼出。

默认快捷键：

```text
Ctrl + D
```

除快捷启动功能外，Sobee 还提供：

- 护眼调色
- 快捷关闭屏幕
- JS 脚本执行
- HTML 文件窗口化运行
- 主进程启动
- 主进程开机启动
- 自动更新
- 配置备份

---

## 2. 第一次启动

首次启动 Sobee 后：

1. 软件会自动隐藏到系统托盘。
2. 右下角会出现 Sobee 蜜蜂图标。
3. 按下 `Ctrl + D` 打开主面板。

如果当前还没有创建任何类目，主面板会显示引导按钮：

- **新建类目**
- **添加文件夹**
- **使用说明**

Sobee 默认启动后隐藏到系统托盘，目前没有单独的「启动后隐藏到托盘」设置。

---

# 3. 主界面

Sobee 主界面主要分为两个区域：

```text
┌──────────────┬────────────────────┐
│              │                    │
│   类目列表    │   快捷方式列表      │
│              │                    │
└──────────────┴────────────────────┘
```

## 3.1 类目区域

左侧用于管理分组。

例如：

```text
开发
├─ Visual Studio
├─ VS Code
└─ Git

娱乐
├─ Steam
└─ 浏览器

工具
├─ 计算器
└─ 截图工具
```

每个类目可以保存独立的快捷方式。

---

## 3.2 快捷方式区域

右侧显示当前类目中的快捷方式。

可以：

- 单击启动
- 右键打开管理菜单
- 从资源管理器直接拖入

---

# 4. 创建类目

创建新的类目：

1. 在类目区域右键。
2. 选择 **新建类目**。
3. 输入类目名称。
4. 保存。

例如：

```text
工作
游戏
开发工具
常用软件
```

创建完成后，选中该类目即可向其中添加快捷方式。

---

# 5. 添加快捷方式

Sobee 推荐直接从 Windows 资源管理器拖入快捷方式，这是最简单的添加方式。

同时支持文件夹扫描和手动修改。

## 5.1 直接拖入

打开 Windows 资源管理器，将以下内容直接拖入 Sobee：

- `.exe`
- `.lnk`
- 文件夹
- 网站快捷方式
- 其他可识别文件

释放后，项目会自动添加到当前类目。

> **推荐使用这种方式添加快捷方式。**

---

## 5.2 扫描文件夹

如果需要批量添加大量软件，可以使用 **增加文件夹**。

操作：

1. 在类目区域右键。
2. 选择 **增加文件夹**。
3. 选择需要扫描的目录。

例如，可以选择：

```text
C:\ProgramData\Microsoft\Windows\Start Menu\Programs
```

Sobee 会递归扫描该目录。

### 扫描规则

假设目录结构如下：

```text
Programs
├─ 软件A.lnk
├─ 软件B.lnk
├─ 游戏
│  ├─ 游戏A.lnk
│  └─ 游戏B.lnk
└─ 工作
   ├─ 软件C.lnk
   └─ 软件D.lnk
```

最终会生成：

```text
Programs
├─ 软件A
└─ 软件B

游戏
├─ 游戏A
└─ 游戏B

工作
├─ 软件C
└─ 软件D
```

也就是说：

- 被扫描的根目录会成为一个类目。
- 子文件夹会分别成为独立类目。
- 文件会被解析为快捷方式，并添加到对应的类目中。
- 子文件夹中的文件不会被全部添加到根目录类目。
- 子文件夹继续存在子文件夹时，会按照相同规则递归处理。

如果 `.lnk` 文件解析后得到的是文件夹目录，也会按照快捷方式进行添加。

### 注意

扫描文件夹会递归处理所有子文件夹。

如果目录中包含大量文件或非常多层的子文件夹，扫描过程会占用较多计算资源。

---

## 5.3 手动创建

当前版本暂时没有单独的「新建快捷方式」菜单。

如果需要手动设置，可以先随便拖入一个文件或文件夹，然后右键该项目，选择 **修改信息**。

可以修改：

- 显示名称
- 路径
- 运行参数
- 图标路径
- 其他快捷方式信息

因此，一般情况下直接从 Windows 资源管理器拖入即可。

---

# 6. 快捷方式管理

右键快捷方式可以打开管理菜单。

常用功能包括：

| 功能 | 说明 |
|---|---|
| 打开 | 启动快捷方式 |
| 管理员打开 | 使用管理员权限运行 |
| 修改信息 | 修改名称、路径、参数、图标等信息 |
| 删除 | 从当前配置中删除 |
| 查找 | 在配置中查找相关项目 |
| 其他功能 | 设置主进程启动等特殊功能 |

不同类型的快捷方式可能显示不同的管理选项。

---

# 7. 图标路径

在 **「修改信息」** 中可以设置：

```text
图标路径
```

图标路径并不要求必须填写 `.ico` 文件。

Sobee 支持直接使用普通图片作为快捷方式图标，会自动识别图片格式并将其转换为快捷方式所需的图标。

例如：

```text
D:\图片\我的图标.png
```

或者：

```text
D:\图片\logo.jpg
```

只要 Sobee 能够识别该图片，就可以将其用于快捷方式显示，因此不需要专门准备 `.ico` 图标文件。

### 7.1 支持的图片格式

Sobee 支持多种常见的光栅、位图以及矢量图片格式：

**光栅 / 位图**

* PNG
* JPG / JPEG
* BMP
* GIF（含动画）
* WebP（含动画）
* ICO
* AVIF
* APNG（动画 PNG）

**矢量**

* SVG

因此，可以直接使用自己制作的图片、软件 Logo、网站图标等作为快捷方式图标。

### 7.2 动态图片

Sobee 支持显示动态图片，例如：

* GIF 动图
* 动态 WebP
* APNG 动图

但是，动态图片在快捷方式窗口显示期间需要持续进行动画处理，因此相比静态图片会消耗更多的：

* CPU
* 内存
* 图像处理资源

当 Sobee 主窗口隐藏时，不会继续进行动态图片的动画计算，因此不会持续占用这部分计算资源。

> **不建议使用 GIF、动态 WebP、APNG 等动图作为快捷方式图标。**

如果只是需要一个普通的快捷方式图标，建议优先使用：

```text
PNG
JPG / JPEG
BMP
ICO
SVG
```

等静态图片格式，可以减少资源占用。

### 7.3 图标示例

例如，可以将：

```text
D:\Icons\chrome.png
D:\Icons\github.svg
D:\Icons\game.webp
```

直接填写到 **「图标路径」** 中。

Sobee 会自动识别对应的图片，并将其显示为快捷方式图标。

因此，**图标路径实际上可以直接填写支持的图片文件，而不局限于 `.ico` 文件。**


# 8. 快速搜索

按下：

```text
Ctrl + F
```

进入搜索模式。

输入关键词，例如：

```text
chrome
```

即可搜索所有类目中的快捷方式。

支持：

- 名称匹配
- 模糊搜索
- 跨类目搜索
- 不区分大小写

按下：

```text
Enter
```

可以快速打开匹配的项目。

---

# 9. 窗口操作

## 9.1 显示 / 隐藏

默认使用：

```text
Ctrl + D
```

显示或隐藏 Sobee 主窗口。

也可以使用：

```text
Esc
```

隐藏主窗口。

---

## 9.2 窗口设置

进入：

```text
托盘图标右键 → 设置
```

可以调整：

- 图标大小
- 是否置顶
- 字体大小
- 全局快捷键
- 其他窗口相关设置

---

# 10. 护眼模式

Sobee 提供独立的护眼窗口。

打开：

```text
托盘图标右键 → 护眼
```

可以调节屏幕的护眼色彩，例如暖色滤镜。

适合长时间使用电脑时使用。

---

# 11. 快捷关闭屏幕

Sobee 支持通过快捷键关闭显示器屏幕。

这里的「关闭屏幕」不是让电脑进入息屏或睡眠状态，而是关闭显示器显示输出。

关闭后，只能通过：

- Sobee 设置的快捷键
- 显示器电源键

等方式重新开启显示。

> 部分显示器可能不支持该功能。
>
> 在虚拟机中使用时，该功能可能没有效果。

---

# 12. JS 脚本

Sobee 支持执行自定义 JavaScript 脚本。

可以用于：

- 自动化操作
- 快速执行命令
- 自定义工具
- 编写自己的小功能

添加 JS 脚本快捷方式后，点击即可执行。

## 12.1 JS 子进程

JS 脚本由独立子进程执行，可以避免脚本运行过程影响 Sobee 主进程。

Sobee 默认不会一直运行 JS 子进程。

只有在执行 JS 脚本时，如果当前没有正在运行的子进程，才会启动子进程。

如果暂时不需要继续运行 JS，可以在快捷窗口中右键选择：

```text
重启执行JS
```

该操作会关闭当前 JS 子进程，并重新启动执行环境，减少不必要的资源占用。

> 使用 JS 脚本功能前，需要在设置中开启「执行 JS」功能。

---

# 13. HTML 文件窗口化

Sobee 支持将普通 HTML 文件作为独立窗口运行。

该功能适合制作一些简单的 HTML / CSS / JavaScript 小工具。

例如：

- 番茄钟
- 随机密码生成器
- 音乐播放器
- 贪吃蛇
- 计算器
- 个人工具页面
- 其他简单的 HTML / CSS / JavaScript 小工具

---


## 13.1 启用主进程打开

对于 HTML 文件，可以通过：

```text
快捷方式右键 → 其他功能
```

启用：

```text
启用主进程打开
```

启用后，Sobee 会使用自己的窗口打开该 HTML 文件，而不是调用 Windows 默认浏览器。

这样一个普通的 `.html` 文件就可以表现得更加类似一个独立的 Windows 小程序。

---

## 13.2 使用方法

只需要：

1. 创建一个 `.html` 文件。
2. 将文件拖入 Sobee。
3. 右键该快捷方式。
4. 打开 **其他功能**。
5. 开启 **启用主进程打开**。
6. 之后从 Sobee 启动该项目。

例如：

```text
我的工具
├─ 番茄钟.html
├─ 随机密码.html
├─ 计算器.html
└─ 贪吃蛇.html
```

无需使用 Electron、Tauri 等桌面应用框架，即可将简单的 HTML 工具作为独立窗口使用。

---

## 13.3 设置 HTML 图标

HTML 文件可以使用自定义图标。

在 Sobee 主窗口中显示时，支持多种图片格式；

如果作为独立窗口运行，则会受到 Windows 窗口图标格式限制。

设置方式：

右键 HTML 快捷方式：

```text
修改信息 → 图标路径
```

可以填写任意可识别的图片。

Sobee 会自动识别图片并将其作为该 HTML 项目的图标。

---

## 13.4 HTML 窗口图标格式限制

需要注意：

快捷方式在 Sobee 主窗口中显示的图标，支持较多图片格式。

例如：

- PNG
- JPG / JPEG
- BMP
- GIF
- WebP
- ICO
- AVIF
- APNG
- SVG

但是，当 HTML 文件通过「启用主进程打开」运行后：

Sobee 会将设置的图标作为 Windows 窗口图标使用。

该图标会显示在：

- 窗口左上角
- Windows 任务栏
- 窗口切换界面


由于 Windows 窗口图标系统的格式限制，此处支持的图片格式较少：

支持：

- ICO
- PNG
- JPG


其他格式（例如）：

- SVG
- GIF
- WebP
- AVIF
- APNG

可能无法作为 HTML 窗口图标正常显示。


因此：

> 如果希望 HTML 工具拥有稳定显示的窗口图标，建议使用 `.ico`、`.png` 或 `.jpg` 文件作为图标路径。


## 13.5 设置默认窗口大小

对于使用 Sobee 打开的 HTML 文件，可以设置创建窗口时的默认：

- 宽度
- 高度

例如：

```text
宽度：800
高度：600
```

这样制作不同类型的 HTML 小工具时，可以根据工具的界面大小设置合适的窗口尺寸。

---

# 14. 主进程启动

Sobee 支持让指定的快捷方式在 Sobee 启动时自动打开。

这个功能与普通的 Windows 开机启动不同。

通过：

```text
快捷方式右键 → 其他功能
```

可以设置：

```text
主进程启动时打开
```

启用后：

> 只要 Sobee 被运行，该快捷方式就会自动打开。

它**不会判断 Sobee 是否是通过 Windows 开机启动运行的**。

例如：

```text
手动运行 Sobee
        ↓
Sobee 启动
        ↓
打开设置了「主进程启动时打开」的快捷方式
```

因此，即使用户是在 Windows 开机完成后手动启动 Sobee，这些快捷方式同样会被打开。

---

## 14.1 可以设置什么？

该功能并不限制快捷方式类型。

例如可以设置：

- 软件
- 文件
- 文件夹
- HTML
- 网站
- 其他可以由 Sobee 启动的快捷方式

因此，可以根据自己的需要，让 Sobee 启动时自动打开常用项目。

---

# 15. 主进程开机启动时打开

Sobee 还提供：

```text
主进程开机启动时打开
```

该功能与「主进程启动时打开」不同。

它的作用是：

> 当 Sobee 确认自己是通过 Sobee 设置的 Windows 开机启动方式运行时，自动打开设置了该功能的快捷方式。

例如：

```text
Windows 开机
    ↓
Sobee 自动启动
    ↓
Sobee 检测到这是开机启动
    ↓
按照设置的顺序打开快捷方式
```

---

## 15.1 必须使用 Sobee 的开机启动

要使用「主进程开机启动时打开」，必须在 Sobee 设置中开启：

```text
开机启动
```

如果通过其他方式让 Sobee 开机启动，例如：

- Windows 任务计划程序
- 启动文件夹
- 注册表
- 其他第三方启动工具

Sobee 可能无法判断当前是否属于 Sobee 设置的开机启动。

因此：

> **只有使用 Sobee 设置中的「开机启动」功能，才能正常使用「主进程开机启动时打开」。**

---

## 15.2 按顺序打开

设置多个「主进程开机启动时打开」快捷方式后，Sobee 会按照设置的顺序依次启动。

例如：

```text
Visual Studio
浏览器
微信
文件夹
HTML 工具
```

可以让这些程序在 Sobee 启动后依次打开。

实际启动过程中，不同程序的启动速度可能存在很大差异。

---

# 16. 启动优先级

对于：

- 主进程启动时打开
- 主进程开机启动时打开

可以使用：

```text
优先级
```

控制快捷方式的启动顺序。

优先级使用整数表示：

```text
优先级越大 → 越早启动
优先级越小 → 越晚启动
```

例如：

| 快捷方式 | 优先级 |
|---|---:|
| 浏览器 | 100 |
| VS Code | 50 |
| 微信 | 20 |
| 文件夹 | 10 |

启动顺序为：

```text
浏览器
↓
VS Code
↓
微信
↓
文件夹
```

---

## 16.1 为什么实际任务栏顺序可能不同？

Sobee 在启动快捷方式之间默认只延迟约：

```text
1ms
```

这样可以尽可能快速启动多个程序。

但是不同程序的启动速度不同。

例如：

```text
程序A：启动需要 3 秒
程序B：启动需要 0.5 秒
```

即使 Sobee 先启动了程序 A，再启动程序 B：

```text
Sobee
 ↓
启动 A
 ↓ 1ms
启动 B
```

B 仍然可能比 A 更早完成启动。

因此，最终 Windows 任务栏中的实际窗口顺序可能与优先级不同。

> **优先级控制的是 Sobee 发起启动的先后顺序，而不是保证程序最终窗口在任务栏中的绝对顺序。**

---

## 16.2 该功能不保证一定成功

「主进程启动时打开」和「主进程开机启动时打开」并不能保证快捷方式在任何情况下都一定成功运行。

例如可能受到：

- 程序本身启动失败
- Windows 权限
- 软件自身限制
- 文件不存在
- 路径发生变化
- 程序启动速度
- 系统资源
- 其他程序或系统环境

等因素影响。

这种情况出现的概率通常很低，但仍然存在。

因此：

> **如果某个程序必须保证一定运行，不建议依赖该功能作为唯一的启动方式。**

该功能主要适合用于方便地自动打开日常使用的软件、文件夹、文件和其他工具。

---

# 17. 自动更新

Sobee 会在启动时检查新版本。

更新流程：

1. 启动 Sobee。
2. 检查 GitHub Releases。
3. 如果发现新版本，则下载更新。
4. 用户确认安装。
5. 自动替换旧版本。

Sobee 每次启动只检测一次，以减少不必要的网络请求和计算。

部分情况下可能需要系统权限，例如软件安装在受保护的系统目录时，更新程序可能需要相应的操作权限。

---

# 18. 配置文件与安全写入

Sobee 的用户配置保存在：

```text
%APPDATA%\Sobee\
```

主要结构（用户真正需要关心的配置都在 `Config` 下）：

```text
Sobee（%APPDATA%\Sobee\）
└── Config                    ← 用户配置（迁移时只需复制此文件夹）
    ├── Settings.json        ← 全局设置
    ├── bee1.json            ← 主面板配置
    ├── bee2.json            ← 护眼窗口配置
    ├── window-state.json    ← 窗口状态
    └── .bak\                ← 保存前配置的安全写入副本（隐藏目录）
```

此外，Sobee 运行目录还可能自动生成 Chromium 引擎的缓存与运行时目录，例如：

```text
Cache
GPUCache
Code Cache
blob_storage
```

这些目录由 Electron 自动管理，属于运行缓存，不属于用户配置。

缓存目录可以随时删除，Sobee 下次运行时会自动重新创建，不会影响用户配置。

## 18.1 配置安全写入

每次保存配置时，Sobee 会采用安全写入机制，避免配置保存过程中出现异常导致原配置文件损坏。

保存过程大致如下：

1. 保存新配置时，先将当前配置文件复制到 `Config/.bak/`（隐藏目录）作为保存前副本。
2. 新配置先写入 `.tmp` 临时文件。
3. 写入完成后，使用 `.tmp` 原子替换原配置文件。
4. 替换成功后，本次保存完成。

这样即使写入过程中发生异常，也可以保留原配置内容。

`.bak` 文件并不是完整的历史备份。

它主要用于：

* 防止配置写入失败导致配置损坏
* 在配置异常时提供恢复依据

它保存的是**最近一次保存前的配置状态**，而不是多个历史版本。

---

## 18.2 查找配置文件目录

不需要手动输入：

```text
%APPDATA%\Sobee\
```

可以进入：

```text
托盘图标右键 → 设置
```

在设置中找到：

```text
配置文件目录
```

可以直接查看当前 Sobee 使用的配置目录。

点击：

```text
打开文件目录
```

即可快速打开该文件夹。

---

## 18.3 配置迁移

如果需要将 Sobee 的配置迁移到另一台电脑，只需要复制：

```text
%APPDATA%\Sobee\Config
```

整个 `Config` 文件夹。

也可以通过：

```text
托盘图标右键 → 设置 → 配置文件目录
```

快速找到该目录。

迁移后，重新启动 Sobee 即可加载原来的配置。

---

# 20. 开机启动

进入：

```text
托盘图标右键 → 设置
```

可以设置：

```text
开机启动
```

开启后，Windows 启动时会自动运行 Sobee。

Sobee 默认启动后会隐藏到系统托盘。

> 「主进程开机启动时打开」依赖 Sobee 自己的开机启动功能。
>
> 如果通过其他方式启动 Sobee，Sobee 无法判断当前是否属于「开机启动」状态，因此无法保证「主进程开机启动时打开」正常工作。

---

# 21. 隐私与网络请求

Sobee 不需要用户注册账号，也不提供云同步功能。

目前会收集：

- CPU 型号
- 内存大小

不会收集带有用户指向性的信息。

Sobee 不包含用于收集用户使用行为的遥测功能。

主要网络请求用于：

```text
GitHub Release 更新检测
```

更新检测只在程序启动时执行一次，以减少不必要的网络请求和性能开销。

---

# 22. 常见问题

## Q：为什么拖入文件没有反应？

如果拖入的是以管理员权限运行的程序，Windows 可能限制普通权限程序接收拖放内容。

可以尝试让 Sobee 使用相应权限运行，然后重新启动 Sobee。

---

## Q：为什么 UWP 应用没有正确图标？

部分 UWP 应用没有独立的 `.ico` 文件。

Sobee 可能会使用默认图标。

如果需要，可以通过：

```text
右键 → 修改信息 → 图标路径
```

手动指定一个图片作为图标。

图标路径并不要求必须使用 `.ico` 文件。

可以填写普通图片，Sobee 会自动识别并使用该图片作为图标。

---

## Q：配置文件在哪里？

可以进入：

```text
托盘图标右键 → 设置
```

找到：

```text
配置文件目录
```

然后使用 **打开文件目录** 功能直接打开配置文件所在目录。

默认目录为：

```text
%APPDATA%\Sobee\Config
```

---

## Q：Sobee 会把我的数据上传到服务器吗？

Sobee 不提供云同步，也不需要账号。

目前仅收集 CPU 型号和内存大小，不收集带有用户指向性的信息。

主要网络请求是 GitHub Release 更新检测，并且只在程序启动时检测一次。

---

## Q：为什么「主进程开机启动时打开」没有生效？

首先确认 Sobee 设置中的：

```text
开机启动
```

是否已经开启。

该功能需要 Sobee 自己设置的开机启动方式。

如果是通过其他方式让 Sobee 开机启动，Sobee 无法判断当前是否属于自己的开机启动，因此无法正常判断是否应该执行「主进程开机启动时打开」。

---

## Q：「主进程启动时打开」和「主进程开机启动时打开」有什么区别？

两者区别如下：

| 功能 | 触发条件 |
|---|---|
| 主进程启动时打开 | 只要 Sobee 被运行，就会打开 |
| 主进程开机启动时打开 | 只有 Sobee 通过设置中的开机启动运行时才会打开 |

例如：

```text
主进程启动时打开
```

即使用户晚上手动打开 Sobee，也会执行。

而：

```text
主进程开机启动时打开
```

只用于 Windows 开机时自动启动 Sobee的场景。

---

# 23. 快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl + D` | 显示 / 隐藏 Sobee |
| `Ctrl + F` | 搜索 |
| `Esc` | 隐藏窗口 |

部分快捷键可以在设置中修改。

---

# 24. 推荐使用方式

可以按照自己的使用习惯创建类目，例如：

```text
开发
├─ VS Code
├─ Visual Studio
├─ Git
└─ Terminal

办公
├─ 浏览器
├─ 文档
└─ 邮件

工具
├─ 截图
├─ 压缩工具
└─ 文件管理

娱乐
├─ Steam
└─ 音乐
```

对于一些临时制作的小工具，则可以直接使用 HTML 窗口化功能。

例如：

```text
我的工具
├─ 番茄钟.html
├─ 随机密码.html
├─ 计算器.html
└─ 贪吃蛇.html
```

还可以利用：

```text
主进程启动时打开
```

让常用工具在 Sobee 启动时自动打开。

如果希望在 Windows 开机时自动恢复工作环境，则可以使用：

```text
主进程开机启动时打开
```

例如：

```text
开机
 ↓
Sobee
 ↓
浏览器
 ↓
VS Code
 ↓
工作文件夹
 ↓
HTML 工具
```

配合 **优先级**，可以控制 Sobee 发起启动请求的先后顺序。

这样既可以保持桌面整洁，又可以快速恢复日常工作环境。

---

# 结束

Sobee 希望把常用的软件、文件和自己制作的小工具集中到一个简单的启动面板中。

**让 Windows 桌面保持简单，让常用工具触手可及。**

如果 Sobee 对你的日常工作有所帮助，欢迎在 GitHub 项目页面点 ⭐ 支持项目。

